type Game @model {
  id: ID!
  host: Player
  party_code: String
  party_limit: Int
  guests: [Player]
  isActive: Boolean!
}

type Player @model {
  id: ID!
  name: String!
  isStoryTeller: Boolean!
  isPed: Boolean!
  isMafia: Boolean!
  isCop: Boolean!
}
